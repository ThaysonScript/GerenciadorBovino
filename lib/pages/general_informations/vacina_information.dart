import 'package:flutter/material.dart';

class VacinaInformation extends StatelessWidget {
  const VacinaInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text("data"),
    );
  }
}
