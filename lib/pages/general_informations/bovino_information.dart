import 'package:flutter/material.dart';

class BovinoInformation extends StatelessWidget {
  const BovinoInformation({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text("data"),
    );
  }
}
